using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public enum Layer
{
    Default = 0,
    TransparantFX = 1,
    IgnoreRaycast = 2,
    Water = 4,
    UI = 5,
    Enemy = 8,
    Player = 9,
    Land = 10
}